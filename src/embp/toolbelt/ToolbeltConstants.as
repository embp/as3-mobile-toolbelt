package embp.toolbelt {
	public class ToolbeltConstants {
		/**
		 * Integer that can be set or checked for identifying an invalid number.
		 * This will work for most cases, but may not be possible to use on apps that
		 * actually generate this number as a valid value.
		 */
		public static const INTEGER_INVALID_VALUE:int = -9001009;
	}
}
